# ELP-USB GS1200P01 global shutter camera parameters for usb_cam (V4L2).
# Verify supported modes: v4l2-ctl --device=/dev/video0 --list-formats-ext
# Global shutter cameras benefit from fixed exposure to reduce motion blur.
# ELP-USB GS1200P01 global shutter camera parameters
# ELP-USB GS1200P01 global shutter camera parameters for usb_cam (V4L2).
# Custom tailored based on v4l2-ctl --device=/dev/video0 --list-ctrls
/**:
  ros__parameters:
    video_device: "/dev/video0"
    framerate: 30.0
    io_method: "mmap"
    frame_id: "camera_link"
    pixel_format: "mjpeg2rgb"
    av_device_format: "YUV422P"
    image_width: 640
    image_height: 480
    camera_name: "narrow_stereo"
    camera_info_url: "package://cnc_perception/config/camera_info.yaml"
    
    brightness: -1
    contrast: -1
    saturation: -1
    sharpness: -1
    gain: -1

    auto_white_balance: false
    white_balance: 4600
    
    exposure_auto: 1
    exposure: 120

    autofocus: false
    focus: -1

